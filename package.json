{
  "name": "lightview-project",
  "description": "Lightview project",
  "license": "Proprietary",
  "devDependencies": {
    "@hubspot/prettier-plugin-hubl": "latest",
    "eslint": "^8.24.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-react": "^7.31.10",
    "prettier": "^2.7.1",
    "yarpm": "^1.2.0"
  },
  "scripts": {
    "start": "cd lightview-project/ui && yarpm start",
    "postinstall": "cd lightview-project/ui && yarpm install",
    "lint:js": "eslint . --ext .js,.jsx",
    "prettier": "prettier . --check",
    "deploy": "hs project upload lightview-project",
    "test": "cd lightview-project && cd ui && yarpm run test"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
